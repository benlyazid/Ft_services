apiVersion: v1 # for versions before 1.9.0 use apps/v1beta2 
kind: Service 
metadata:
  name: grafana 
  annotations:
    metallb.universe.tf/allow-shared-ip: shared
spec: 
  selector: 
    app: grafana 
  ports:
    - port: 3000
      name: tcp
      targetPort: 3000
  type: LoadBalancer